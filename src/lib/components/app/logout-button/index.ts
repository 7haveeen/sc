import type { ButtonProps, ButtonSize, ButtonVariant } from "./logout-button.svelte";

import Root, {

  buttonVariants,
} from "./logout-button.svelte";

export {
  type ButtonProps,
  type ButtonSize,
  type ButtonVariant,
  buttonVariants,
  //
  Root as LogoutButton,
  type ButtonProps as Props,
  Root,
};
